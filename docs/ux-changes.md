# UX changes of the form platform migration

The golden transcripts under `tests/behavioral/golden/` are the record of what an
admin sees today, path by path. A migration of the engine is invisible to admins
unless a change is listed here first (review II.3). Every row names the golden it
moves; the golden test accepts the row's id in `allowed=` and nothing else.

Classes: `invisible` (ids, internal encodings: never listed, the normalizer drops
them), `cosmetic` (a footer, an icon), `behavioural` (a new notice, a different
message count, a different choreography).

| id | form / screen | before | after | class | reason | golden affected | since |
|---|---|---|---|---|---|---|---|
| ux-1 | every form, any button or select | a click on an outdated screen is routed to the current handler (deterministic ids) or fails silently (random ids); a double click on Done / Confirm runs the step twice | a short ephemeral notice ("this screen is out of date, here is the current one") and the current screen is redrawn once; the second click of a double click is ignored | behavioural | dedup by interaction id and revision in the adapter (review 4.6, plan 3.5) | none of the recorded paths clicks twice or on an old screen; new scenarios in Phase C | Phase D |
| ux-2 | every form, after 30 minutes without a click | the buttons stay on the message and every click fails with "This interaction failed" | the message loses its buttons and says the form expired, in the admin's locale | behavioural | expiry becomes a visible transition (review C2, H6) | none of the recorded paths waits for a timeout; `scenario.expire()` in Phase C | Phase D |
| ux-3 | options steps, confirming with nothing selected | a public `channel.send` in the channel where the command ran | an ephemeral error embed, like every other validation error | behavioural, a bug fix | one error channel (review G13) | none of the recorded paths confirms an empty options step | Phase D |
| ux-4 | every transition from a card or gallery back to an embed screen | the old message is deleted first, then the new one is sent; when the delete fails the admin loses the screen (H11) | the new message is sent first, then the old one is deleted | invisible when it succeeds, behavioural when the delete fails | one replacement choreography in the effect executor (review P3, H11) | every golden with `delete` before `followup_send`: block_links edit and remove, default_roles edit, twitch and youtube add, edit and remove, birthday edit and remove, every manager pause / disable | Phase D |
| ux-6 | welcome_messages, the design gallery | the gallery's buttons read Cancel, Back: the one screen where Cancel is not last | Back, Cancel, like every other screen | cosmetic | one button order for every screen (`cancel_last`) | `welcome_messages/setup_happy`, `setup_custom_image`, `setup_back`, `setup_cancel_keep`, `setup_cancel_discard`, `manager_edit_one_step` | Phase D |
| ux-8 | the review screen, values of `bullet` style (twitch and youtube notification messages, welcome messages) | the review lists them numbered (`1.`, `2.`) while the panel lists the same values with bullets: two formatters with the styles swapped | bullets on the review too | cosmetic, a bug fix | one formatter per style (review G6) | `notifications_twitch/setup_happy`, `notifications_youtube_video/setup_happy`, `welcome_messages/setup_happy` | Phase D |
| ux-9 | modals opened to edit a saved value (twitch and youtube notification messages, stream_elements streamer, birthday day) | some open empty, some open with the saved value: each modal decided on its own | every modal opens with the value it edits | cosmetic | the modal is a render of the session's answers (review I3) | `notifications_twitch/manager_edit_one_step`, `notifications_youtube_video/manager_edit_one_step` | Phase D |
| ux-10 | `/setup` and the greeting's dashboard button | an embed with one bold line per feature, the command on the next line, and six buttons below the list | a Components V2 card with Keiko's picture beside a three-line header (a 🐶 title, a one-sentence intro, and a subtext note saying these are the moderation and notification features and that `/help` has the rest); features grouped under "to set up" first and "configured" second with their counts; each row is the feature's name plus one subtext line with what it does and its command (and "paused" when it is), with a Set up or Manage button beside it; no status circles; the report footer; below the card, outside its frame, a row of Commands (the /help list, privately), History (every feature's recent changes), Permissions (what Keiko is missing for each configured feature) and Support (a link to the official server) buttons | cosmetic | the screen is most servers' first contact with Keiko; a first card redesign said each status three times over three lines, left a blank band beside the picture, and listed what was already done before what was left | none (pinned by tests/behavioral/contracts/test_setup_dashboard.py) | logs and setup train |
| ux-11 | every Components V2 form screen: manager panels, setup cards, the design gallery and pickers | the screen's own buttons (Pause, Disable, Add, History, Help, Done, Back, Cancel) sit inside the card's frame, above the footer | the card holds the content and closes with the footer; the screen's own buttons sit below it, outside the frame, as on `/setup`; a button that belongs to one part of the card (a section's buttons, the Edit beside a group, a design's Select, a picker's options) stays inside | cosmetic | one place for a screen's buttons on every card, the `/setup` layout of ux-10 | 58 goldens, every one with a card: block_links (13), default_roles (5), notifications_twitch (7), notifications_youtube_video (7), reminders_birthday (12), stream_elements_commands (5), welcome_messages (9); re-recorded after passing under a canonicalizer that only folded the rows back into the card | logs and setup train |
| ux-12 | welcome_messages: the design gallery and the banner a new member gets | the "custom image only" example was a 28.3 MB gif on an outside host, slow and at times "image not found"; the blur example waited on a 1 MB download; a server without an icon, or whose custom background link had expired, got no welcome message at all | the example is a 2.4 MB gif served from Discord, the blur example is drawn from a local picture, both banners are drawn together; a server without an icon gets a plain background in the house colour, an expired Discord background link is refreshed before drawing, and any background that still cannot be loaded falls back to the plain one | behavioural | the gallery is an admin's first look at the feature, and a welcome message that silently never arrives is the worst outcome of the feature | none (the goldens count gallery media, not urls; pinned by tests/behavioral/regressions/test_welcome_design_previews.py) | welcome previews train |
| ux-13 | block_links: the mode picker, the card, the review and the panel | the modes read "🚫 Block all, with exceptions" and "✅ Allow all, block only some"; an admin who blocks every link read "with exceptions" as something still to set up | "🚫 Block all" and "✅ Allow all"; the picker line of Block all says exceptions are optional, and the exceptions keep their own lines on the card and the panel (None when empty) | cosmetic | the label described what an admin may add, not the mode | the 13 block_links golden transcripts; re-recorded after passing under a canonicalizer that only folded the labels and the picker text | panel and cards train |
| ux-14 | every form, the dropdown that picks what to edit or remove (manager and review) | the dropdown had no button: the only way back to the panel or the review was to pick something or run the command again | a Back button below the dropdown returns to the panel in the manager and to the review in the setup, without changing anything | behavioural | every picker of a card already had a way back; this one did not | `block_links/manager_remove_item`, `notifications_twitch/manager_edit_one_step` and `manager_remove_item`, `notifications_youtube_video/manager_edit_one_step` and `manager_remove_item`, `reminders_birthday/manager_edit_one_step` and `manager_remove_item`, `stream_elements_commands/manager_edit_one_step`, `welcome_messages/manager_edit_one_step`; re-recorded after passing under a canonicalizer that only dropped the Back button | panel and cards train |
| ux-15 | default_roles: the roles step and the Sync button of the panel | one line said to choose roles for new bots and members, and only the dropdown placeholders told the two apart; confirming with both dropdowns empty saved an empty setup; Sync assigned roles to every member on one click and answered with a bare green embed | the step lists "1. Roles for bots" and "2. Roles for members" with when each applies and says one may stay empty; confirming with both empty shows the selection-required error; Sync first asks, on a private message with Confirm and Cancel, and spends its cooldown only when confirmed; the waiting and result embeds carry Keiko's picture, the house colour, a footer, and the result counts members and bots | behavioural, a bug fix | the only setting of the feature must say what each dropdown is for, and an action that touches every member must say so before it runs | `default_roles/setup_happy` (both locales), `setup_cancel_keep`, `setup_cancel_discard`, `manager_edit_one_step`; re-recorded after passing under a canonicalizer that only folded the step description; the refusal and Sync are pinned by `test_a_required_multi_select_refuses_confirm_when_every_select_is_empty`, `test_an_aside_that_asks_first_spends_its_cooldown_only_when_confirmed` and `test_the_sync_result_reads_like_keiko` | panel and cards train |
| ux-16 | notifications_twitch and notifications_youtube_video: the streamer and youtuber modal; stream_elements_commands: the streamer modal | the placeholder was a bare `shroud` / `pewdiepie` and the hint said "only the username"; a nick typed as `@gaules` was not found | the placeholder reads "your nick (e.g. @shroud)" / "your channel (e.g. @pewdiepie)" and the hint says with or without @; the lookup, the validator and the saved item use the nick without spaces, a leading @ or capitals (`normalize: handle`) | behavioural | the placeholder invites the @, so the @ must work | `notifications_twitch` and `notifications_youtube_video` `setup_happy` (both locales), `setup_back`, `setup_cancel_keep`, `setup_cancel_discard`, `setup_validation_error_and_recover`; re-recorded after passing under a canonicalizer that only folded the hint and the placeholder; the @ is pinned by `test_a_streamer_typed_with_an_at_sign_is_found_and_saved_without_it` | panel and cards train |
| ux-17 | `/setup`, `/help`, and the saved, paused, unpaused, disabled and history messages of stream_elements_commands | Stream Elements was missing from `/setup`; `/help` never mentioned `/setup`; Stream Elements messages named a command that does not exist (`/stream elements commands`) | `/setup` lists Stream Elements with its command and a Set up or Manage button, and its note says integrations too; `/help` says admins can use `/setup` and carries a Setup button that opens it privately (a member gets the admin-only notice); the messages name the real command (`/integrations streamelements commands`) | behavioural | the integration was a command nobody found, and `/help` is where people look first | the seven stream_elements_commands goldens (`setup_happy` in both locales, `manager_edit_one_step`, `manager_pause`, `manager_unpause`, `manager_disable`, `manager_history`); re-recorded after passing under a canonicalizer that only folded the command name; `/setup` and `/help` are pinned by `tests/behavioral/contracts/test_setup_dashboard.py` | panel and cards train |
| ux-18 | a modal whose value is looked up outside Discord (the Twitch or YouTube name, the StreamElements streamer), in the setup and when edited from the panel | the bot looked the name up before answering the modal, so a slow Twitch or YouTube answer could lose Discord's three seconds; saving an edit of that field showed "Keiko is thinking" first | the modal is answered first (a silent defer) and then the name is looked up; an edit saved from such a modal no longer shows the thinking placeholder before the saved message | behavioural | a lookup can take longer than Discord waits for a modal | `stream_elements_commands/manager_edit_one_step`; re-recorded after passing under a canonicalizer that only dropped the thinking defer; the order is pinned by `test_a_modal_that_needs_a_lookup_is_answered_before_the_lookup_runs` | panel and cards train |
| ux-19 | every manager panel | settings were plain `**Title:** value` lines; only a card, a multi-select or a list had a heading and an Edit beside it, every other step (a channel, a design, a modal) was reached through a global Edit and a dropdown; the intro read "Enabling this feature allows me to..."; the birthday rows had no group and kept the global Edit | the panel opens with `manager_description`, written as what the feature is doing; every step is a block with an Edit beside what it opens; every line leads with its own emoji (the section, select or step icon, the frisbee when none is declared); a one-line block has no heading; the global Edit only remains when a feature row names no step; the birthday rows sit under the settings card and the birthday list | behavioural | an Edit beside what it opens (decided with Lucas, 2026-09-15), and a panel description that reads as the feature being on | the 42 manager goldens of the seven forms; re-recorded after reading each form's new panel (the change is structural: sections, headings and buttons move, so no canonicalizer can fold it); `stream_elements_commands`, `welcome_messages` and `reminders_birthday` `manager_edit_one_step` now click the Edit beside the step instead of the global Edit and its dropdown; pinned by `test_every_visible_step_is_a_panel_group_with_its_own_edit`, `test_every_panel_line_leads_with_its_icon_or_the_frisbee`, `test_a_one_line_group_has_no_heading`, `test_feature_rows_group_by_the_step_that_owns_their_key` and the manager panel limits contract | panel and cards train |
| ux-20 | every setup review ("Alright?") | an embed listing every answer as a frisbee line with its value in bold (a list of messages printed `**` around a code block), an Edit that opened a dropdown of steps, and the saved message edited over it | a Components V2 card with the same blocks as the manager panel: every step a block with an Edit beside what it opens, a line per setting led by its emoji, lists as their items; Add, Remove, Preview, Confirm and Cancel below the card; the Edit beside a block returns to the review; confirming replaces the card with the saved message | behavioural, a bug fix (the bold around a block) | one screen shape for reading settings, whether they are being set up or managed | the 17 setup goldens that reach the review (`setup_happy` of the seven forms in both locales, `welcome_messages/setup_custom_image`, `reminders_birthday` and `stream_elements_commands` `setup_validation_error_and_recover`); re-recorded after reading each form's review card, the change is structural; pinned by `test_the_review_is_a_card_with_the_panel_groups_and_its_buttons_below`, `test_edit_beside_a_review_group_returns_to_the_review`, `test_confirming_a_card_review_replaces_it_with_the_enabled_message`, `tests/behavioral/regressions/test_review_values.py` and the setup review limits contract | panel and cards train |
| ux-21 | default_roles: the manager panel and the setup review | one block with one Edit that opened both dropdowns and a Confirm | each dropdown line has its own Edit: it opens only that dropdown, with Back, and saves as soon as a role is chosen, leaving the other dropdown as it was | behavioural | two settings on one screen read as two things to change (decided with Lucas, 2026-09-15) | `default_roles/setup_happy` (both locales), `manager_edit_one_step` (the path clicks the Edit beside the members dropdown and no longer confirms), `manager_pause`, `manager_unpause`, `manager_disable`, `manager_history`; re-recorded after reading the new panel and review; pinned by `test_editing_one_default_roles_select_commits_it_from_its_own_edit` | panel and cards train |
| ux-22 | welcome_messages: the setup, the manager panel and the review | a channel pick, then a design gallery, then an image upload for custom designs, then a notes screen, then a five-field modal, then the review: six screens in a row; the panel had a block per step | one card with Channel (the only thing to choose), Banner design (opens the gallery with previews, Server Image by default), Custom image (shows only for a custom design, and is then required) and Messages (title, three random messages and footer, already filled with the defaults); the variables are explained on the card; on the panel every card line has its own Edit that opens just that part; the design gallery opened from the panel has its previews | behavioural | fewer steps and visible defaults for the most common setup (decided with Lucas, 2026-09-15); the saved document keeps the keys and shapes the welcome sender reads | the 11 welcome_messages goldens, their setup paths rewritten for the card (`customize:0` for the channel, `customize:1` and `design:` for the design, `customize:2` for the image, `customize:3` for the messages) and `manager_edit_one_step` clicking the Edit beside the channel; pinned by `test_the_welcome_card_saves_the_document_the_welcome_service_reads`, `test_a_welcome_custom_image_is_required_only_for_custom_designs`, `test_the_design_picker_opened_from_the_welcome_panel_shows_previews` and the welcome card limits contract | panel and cards train |
| ux-23 | notifications_twitch and notifications_youtube_video: adding, editing and reviewing a streamer or youtuber, and the manager panel | each item ran four screens in a row: a channel pick, a name modal, a notes screen and a three-message modal; the panel had one block for the whole list, whose Edit opened a dropdown to pick the item | each item is one card with Text Channel, the Streamer or Youtuber (the nick, with or without @, looked up and validated inside the card) and Notification Messages (already filled with the default), with the variables explained on the card; the panel and the review show one block per item, each with an Edit that opens that item's card | behavioural | fewer steps and visible defaults (decided with Lucas, 2026-09-15); the saved items keep the keys and shapes the notifiers read (messages as one ;-joined text) | the 13 notifications_twitch and 13 notifications_youtube_video goldens, their setup and manager paths rewritten for the card (`customize:0` channel, `customize:1` name, `customize:2` messages, Done) and `manager_edit_one_step` clicking the Edit beside the first item; pinned by `test_an_item_card_saves_the_item_the_notifier_reads`, `test_editing_an_item_from_its_own_edit_moves_the_subscription`, `test_a_list_item_card_asks_for_the_lookup_of_its_streamer_modal` and the Twitch card limits contract | panel and cards train |
| ux-24 | stream_elements_commands: the setup review | "Whenever someone uses a command, I'll fetch the response from StreamElements", with no sign of what would load | "I found **N** active commands in `streamer`'s StreamElements!", counted by the streamer lookup; a streamer with no active commands reads as that rather than as "I found 0"; when StreamElements cannot be reached, a line saying so instead of a number | behavioural | the admin sees whether the streamer has commands before saving | `stream_elements_commands/setup_happy` (both locales) and `setup_validation_error_and_recover`; re-recorded after reading the review; pinned by `test_the_stream_elements_lookup_counts_enabled_commands`, `test_a_stream_elements_outage_leaves_no_count`, `test_a_lookup_answer_is_kept_hidden_and_never_saved` and the two scenarios of `tests/behavioral/regressions/test_stream_elements_outage.py`, which read the review itself | panel and cards train |
| ux-25 | every manager panel: a list with no item yet (block links without custom links, a notifications list emptied) | the list block kept its own Edit; clicking it built a dropdown with no option and Discord refused the whole message (`50035 Invalid Form Body`), so the panel showed an error instead of a screen | a list with no item shows no Edit of its own (the global Edit comes back for the other settings, and Add is still below the card), and any picker that would have nothing to pick says the panel is out of date and redraws it instead of drawing an empty dropdown | behavioural, a bug fix | found by Lucas on the local bot (2026-09-15) | none (every golden seeds its list with items); pinned by `test_a_list_with_no_items_has_no_edit_of_its_own` and `test_an_edit_with_nothing_to_pick_redraws_the_panel` | panel and cards train |
| ux-26 | the first screen of block_links, notifications_twitch, notifications_youtube_video and welcome_messages | the "Settings" list came out empty for welcome, Twitch and YouTube once their steps became one card, and block_links listed only the steps outside its card: a card field is listed only when it declares a description | every field of the card the form will ask for is listed, each with one line saying what it is | cosmetic | the list is what tells an admin what the form is about to ask | the 24 setup goldens of the four forms that render the intro; re-recorded after checking, line by line, that every difference falls inside the settings list; pinned by `test_the_intro_lists_every_field_of_the_cards_it_reads` | panel and cards train |
| ux-27 | every review and panel: a list whose gate was answered "later" (the birthday list, block links custom links) | adding the first item with the review's Add button saved it, but the review still showed no list at all: a step whose condition is false was dropped even when it had something saved | a setting with something saved is always shown, condition or not, and the Edit beside it opens it: the edit picker follows the same rule as the panel; one conditioned out with nothing saved stays hidden | behavioural, a bug fix | found by Lucas on the local bot (2026-09-16) | none (no recorded path answers the gate with "later" and then adds); pinned by `test_the_first_member_added_from_the_review_is_listed_on_the_review` and `test_edit_beside_a_list_kept_past_its_gate_opens_that_list` | panel and cards train |
| ux-28 | default_roles: the roles step, the refusal when both dropdowns are empty, and the Sync question | the step closed with "You can leave one of them empty, just not both!"; confirming with both dropdowns empty answered "Please pick an option before confirming", which names a selection nobody was asked for; the Sync question said it would give the default roles to everyone without saying which roles | the step lists the two dropdowns and stops there; the refusal reads "Please fill one of the fields before confirming"; the Sync question names the roles every member and every bot will end up with, or None when a dropdown is empty | cosmetic, copy | an empty multi-select is a field to fill, not an option to pick, and an action that touches every member must say what it will leave behind | `default_roles/setup_happy` (both locales), `setup_cancel_keep`, `setup_cancel_discard`, `manager_edit_one_step`; re-recorded after checking that every changed line differs only by the removed sentence; pinned by `test_a_required_multi_select_refuses_confirm_when_every_select_is_empty` and `test_an_aside_that_asks_first_spends_its_cooldown_only_when_confirmed` | panel and cards train |
| ux-29 | block_links: the blocking mode picker, its buttons, and the mode line on the card, the panel and the review | the modes read "🚫 Block all" and "✅ Allow all": "Allow all" named what Keiko does with most links, not what the admin is setting up, and said nothing about the links it would still block | the modes read "🚫 Block all" (free some websites and links afterwards) and "🔗 Block some" (I block only the websites and links you pick); each picker line explains the mode in the words of what will be configured | cosmetic | the label must say what the admin is about to set up | none (the recorded transcripts do not store the picker screen text); pinned by `test_mode_picker_explains_each_mode_in_its_own_line` and the block_links flow scenarios | panel and cards train |
| ux-30 | block_links: the manager panel and the setup review | the popular websites sat inside the link settings block and the custom links were a block of their own at the bottom, each reached by its block Edit; nothing said the two answer the same question | both sit under one "Exceptions" heading ("Blocked websites and links" while the mode blocks only some), each keeping its own button: "Free a famous website" opens the popular sites, "Free specific links" opens the list | behavioural | the two settings are the same decision, and the heading follows the mode so it never reads as an exception to nothing | the 9 block_links goldens that draw the panel or the review (`setup_happy` in both locales, `manager_edit_one_step`, `manager_add_item`, `manager_remove_item`, `manager_pause`, `manager_unpause`, `manager_disable`, `manager_history`); re-recorded after reading the new panel, the change is structural | panel and cards train |
| ux-31 | the Twitch streamer, YouTube channel and StreamElements streamer modals | the placeholder read "your nick (e.g. @shroud)" / "your channel (e.g. @pewdiepie)", repeating what the label right above it already says | the placeholder is the example alone: "e.g. @shroud", "e.g. @pewdiepie" | cosmetic | the label says what to type, the placeholder shows what it looks like | none (the recorded transcripts do not store modal placeholders) | panel and cards train |
| ux-32 | stream_elements_commands: the setup review and the manager panel | the review said how many commands it found but never which ones; the panel said nothing about what was loaded and offered no way to read the commands | the review names the first commands it found (up to five), and the panel carries a "Commands loaded" block with the total for that streamer, or a line saying StreamElements could not be reached, plus a Commands button that opens the paginated list privately | behavioural | StreamElements reports no usage count, so the five are the first the API returns, not the most used | the 5 stream_elements_commands manager goldens (`manager_edit_one_step`, `manager_pause`, `manager_unpause`, `manager_disable`, `manager_history`); re-recorded after reading the new panel (the setup goldens do not move: with StreamElements unreachable the review keeps its old sentence); pinned by `test_the_stream_elements_panel_says_how_many_commands_it_answers`, `test_the_review_names_the_first_commands_the_lookup_found` and `test_the_stream_elements_lookup_counts_enabled_commands` | panel and cards train |
| ux-33 | reminders_birthday: the member card, the setup review, and the message the server gets on the day | there was no way to see the celebration before saving it, and the message arrived with no reaction | the member card and the review carry a Preview button that renders the celebration exactly as it will arrive (that member message, or the default one) privately; the message Keiko sends on the day arrives with a 🎉 reaction | behavioural | a message nobody can see before the day is a message nobody trusts, and the reaction invites the server to join | the 4 reminders_birthday goldens that draw the member card or the review (`setup_happy` in both locales, `setup_validation_error_and_recover`, `manager_add_item`); re-recorded after checking the diff only adds the Preview button (90 lines added, none removed); pinned by `test_the_member_card_offers_a_preview_of_that_member_message`, `test_the_birthday_feature_previews_the_celebration` and `test_the_birthday_message_arrives_with_a_party_reaction` | panel and cards train |
| ux-34 | welcome_messages: the setup card, the panel, the review and the design gallery | the card asked for the design before the messages, the gallery drew every design with the fixed word "WELCOME", and the only way to see the result was the Preview on the review, which showed one message drawn at random | the card asks for the channel, then the messages, then the design; the gallery draws each design with the title that is saved; the card itself carries a Preview button, and the preview walks through the three written messages with a Next message button, showing the custom image when the design uses one | behavioural | the design is chosen looking at the text it will carry, so the text comes first (Lucas, 2026-09-16); a preview that picks one message at random never shows the other two | the 6 welcome_messages goldens that draw the card (`setup_happy` in both locales, `setup_custom_image`, `setup_back`, `setup_cancel_keep`, `setup_cancel_discard`); re-recorded after reading the new card, the change is structural (the sections move and the Preview button appears); pinned by `test_the_preview_walks_through_every_written_message` and the welcome card limits contract | panel and cards train |
| ux-35 | notifications_twitch and notifications_youtube_video: each item on the manager panel and on the review | the only way to remove one streamer or channel was the Remove button below the card, which opened a dropdown listing every item | each item block also carries a "Remove this" button of its own, in red, right under its lines; it asks on a private message before removing anything, and Keep editing closes it without touching the list; the Remove below the card stays as it is | behavioural | removing the item you are looking at should not mean picking it again from a list (Lucas, 2026-09-16) | the 18 notifications_twitch and notifications_youtube_video goldens that draw the panel or the review; re-recorded after reading the new panel (242 lines added, none removed: the button row of each item); pinned by `test_each_item_carries_a_remove_of_its_own_that_asks_before_removing` | panel and cards train |
| ux-36 | notifications_twitch and notifications_youtube_video: the item card and the review | the card showed the nick as plain text, and there was no way to read the announcement before saving it | once the nick is typed, the card carries the streamer or channel picture beside its title, and both the card and the review carry a Preview button that shows the announcement as it will arrive, one written message per click | behavioural | the picture confirms the lookup found the right account, and the preview shows what the server will read (Lucas, 2026-09-16) | the 16 notifications_twitch and notifications_youtube_video goldens that draw the card or the review; re-recorded after reading the new card (the header lines fold into a section with the picture, and the Preview button appears: 543 lines added, the 78 removed are the header text lines that moved into that section); pinned by `test_the_card_shows_the_picture_of_the_streamer_just_typed`, `test_the_item_card_offers_a_preview_of_the_notification`, `test_the_message_preview_walks_through_every_text` and the two scenarios of `tests/behavioral/regressions/test_review_preview.py`, which read what a preview opened from the review says | panel and cards train |
| ux-37 | the welcome preview and the Twitch or YouTube notification preview, when only one message is written | the preview answered nothing and logged a crash: with no second message to page through, the bot handed Discord a button row that did not exist | the preview opens with the single message and no Next button | behavioural, a bug fix | found by Lucas on the local bot (2026-09-16) | none (no recorded path previews a single message); pinned by `test_a_preview_with_one_message_never_sends_a_none_view` and `test_the_welcome_preview_with_one_message_never_sends_a_none_view` | panel and cards train |
| ux-38 | the Preview button of a Twitch or YouTube item card | the preview opened from an item card found none of that item answers and Discord refused an empty message; the birthday member preview showed the default message for the same reason | a preview reads the fields of the item it was opened from, whether it was opened from the item's own card or from the review that lists it, and a preview with nothing written says so in its own words instead of borrowing the empty settings value | behavioural, a bug fix | found by Lucas on the local bot (2026-09-16) | none (no recorded path presses Preview inside an item card); pinned by `test_the_preview_of_an_item_card_reads_the_fields_of_that_item` and `test_a_preview_with_nothing_written_still_says_something` | panel and cards train |
| ux-39 | notifications_twitch and notifications_youtube_video: each item on the manager panel and on the review | each item block carried a red "Remove this" button of its own, added in ux-35 | the item blocks carry their Edit alone again; removing an item is the Remove button below the card, as it was before ux-35 | behavioural | tried on the local bot and taken back out: a red button under every item crowded the panel (Lucas, 2026-09-16) | the 18 notifications_twitch and notifications_youtube_video goldens that draw the panel or the review, back to their ux-36 content (286 lines removed, none added) | panel and cards train |
| ux-58 | block_links: the Exceptions screen, and any screen that saves without leaving | the first click saved and every click after it did nothing: the adapter reported every commit back to the engine under the same id (`commit:<session>`), which the engine drops as an event it has already seen, so the session stayed on COMMITTING and answered the next click "busy"; each click also left an ephemeral "Keiko is thinking..." message on screen forever, because the executor announces the thinking state before an `edit` commit and a screen that redraws itself never sends the message that would answer it | every click on the screen saves and redraws it, Voltar keeps working, and none of them says it is thinking | behavioural, a bug fix | found by Lucas on the local bot (2026-09-16), on the screen ux-57 introduced | none (no golden path changes the same screen twice); pinned by `test_two_changes_in_a_row_on_the_same_screen_both_save` and `test_a_change_that_keeps_its_screen_never_says_it_is_thinking` | panel and cards train |
| ux-59 | the onboarding greeting a new server gets | the card offered four features (welcome messages, default roles, Twitch, YouTube), because the greeting kept a list of its own while `/setup` read another one with seven | the card offers the same seven `/setup` does, both built from the one registry | behavioural | two registries of the same thing had drifted: block links, birthdays and Stream Elements were added to `Commands.SETUP_FEATURES` and never reached the greeting, so the first screen a server sees advertised less than the bot does and `guild.greeting_sent` under-reported what was offered | none (no golden path posts the greeting); the count both call sites read is now the constant itself | platform cleanup |
| ux-57 | block_links: the Exceptions block on the manager panel, and the screen its Edit opens | the block printed every custom link in full, with a "#1" heading and an icon on every field of every item; the screen behind the Edit was a bare list with no word of what it was for, one button that opened a dropdown of popular websites (which did not even look like it took more than one) and another that did exactly what the Add below it did, and answering either of them dropped the admin back on the panel | the block names the links in a single line; the screen says what the exceptions are for in the words of the mode in force, carries the popular websites as buttons that turn on and off where they are read, and gives every link its own Edit and Remove right under it, with one Add for a new one; turning a website on, removing a link or adding one saves and keeps the screen, and Back is what returns to the panel  The screen belongs to the manager: a setup review draws the same rows in place, because the screen saves as it goes and a setup saves nothing before Confirm. The panel keeps its own Edit whenever the list is longer than the three items the screen buttons, so every link stays reachable.| behavioural | the exceptions are one decision the admin works through, not a list to be navigated away from (Lucas, 2026-09-16); Discord refuses a message over forty components, so the screen carries the first three links in full and says how many are left, and the panel's own Edit and Remove still reach every one of them | the 7 block_links goldens that draw the panel, for the one line the links now take; the screen itself is pinned by `test_the_exceptions_are_one_block_that_opens_a_screen_of_its_own`, `test_a_popular_website_turns_on_where_it_is_read`, `test_a_website_that_is_on_turns_off_on_the_same_click`, `test_removing_one_link_asks_first_and_stays_on_the_screen`, `test_back_from_the_exceptions_screen_returns_to_the_panel` and the limits contract with a list of 25 links | panel and cards train |
| ux-56 | every command that opens a form | ux-52 answered every command with a deferral, so "Keiko is thinking" flashed even when the panel was ready in a moment | nothing is deferred while the answer is on time: a clock deferring at 1.5 seconds steps in only when reading or drawing is about to run past the three seconds Discord gives, and on a button that deferral is invisible anyway | behavioural | the thinking state belongs to work that takes time, and paying it on every command to protect the rare slow one is the wrong trade (Lucas, 2026-09-16) | the 73 goldens ux-52 had moved go back to their previous content, byte for byte, and only the drawing of a banner still defers (welcome_messages/setup_custom_image); pinned by `test_a_command_that_answers_in_time_never_says_it_is_thinking` and `test_a_command_that_runs_late_is_answered_before_the_clock_runs_out` | panel and cards train |
| ux-55 | stream_elements_commands: the first screen, the review and the command reply | the copy named the prefix as `ks!`, written by hand, while the bot answers to the prefix in its own configuration, so a server whose prefix differs read instructions that did not work | every line that names the prefix reads it from the configuration, so the example, the review and the reply all say the same thing | cosmetic, a bug fix | found by Lucas on the local bot (2026-09-16): the review said `ks!` and the Keiko version of each command said `ke!` | none (the test bot's prefix is the same `ks!` the copy had); pinned by the compiled form and the intro rendering through the token | panel and cards train |
| ux-54 | welcome_messages: the banner on the card, after a custom picture is sent | the line showed the picture itself, which is not what a member receives: the designs that blur a background were still drawn over the example | the banners are drawn again over the picture the admin just sent, so the card shows the banner a member would get; the design that is only the picture keeps showing it | behavioural | a preview that is not the real thing is worse than none (Lucas, 2026-09-16) | none on screen (the transcripts record a media block, never its url); `welcome_messages/setup_custom_image` records the defer the redrawing needs; pinned by `test_a_blurred_design_draws_the_banner_over_the_sent_picture` | panel and cards train |
| ux-53 | welcome_messages: the Preview button while the card is open | Preview answered nothing at all when pressed before touching any section: until the first change a card holds no draft, so the preview found none of the values the admin was reading on the screen | Preview draws what the screen shows, defaults included, from the first moment the card opens | behavioural, a bug fix | found by Lucas on the local bot (2026-09-16), still failing after ux-48 because that fix only reached a draft that already existed | none (no golden path presses Preview); pinned by `test_a_card_nobody_touched_yet_still_shows_its_defaults` | panel and cards train |
| ux-52 | every command that opens a form, and every screen that waits for a drawing | the panel raced Discord's three seconds: the bot read the saved document first and answered after, so a slow read or a slow link lost the interaction (10062) and the command answered nothing | the interaction is answered first and the screen arrives as a followup, so reading, drawing and network latency no longer cost the command | behavioural, a bug fix | found by Lucas on the local bot (2026-09-16), where renders took 1.3s, 2.9s and 8s under Discord rate limits and a VPN | all 82 goldens gain the defer that answers the click; proved mechanically: with defers, sequence numbers and the name of the HTTP call removed, 73 transcripts are identical and the other 9 are the ux-51 block; pinned by `test_opening_a_command_answers_before_it_reads_anything` | panel and cards train |
| ux-51 | block_links: the Exceptions block on the manager panel and the review | the two exception settings sat between the link settings and the permissions, each carrying its own button on the panel, and only one of the two worked | the block sits last, under the permissions, and holds one Edit; that Edit opens a screen of its own where the popular websites and the admin's own links each have their button, with Add and Remove for the list and a way back | behavioural | the exceptions are one decision with two halves, and the panel is the wrong place to fan them out (Lucas, 2026-09-16) | the 9 block_links goldens that draw the panel or the review; re-recorded after reading the new panel, and proved against the old ones: only these 9 changed on screen, the other 73 differ by the defer of ux-52 alone | panel and cards train |
| ux-50 | every embed the bot sends: forms, errors, responses, the report DM and the admin screens | the closing line sat in the embed footer, which renders no markdown, so it read literally as "Use o comando `/reportar` para me contar um bug", backticks and all | the line closes the body as subtext, where `/reportar` renders as the command it is, the same way the cards already showed it | cosmetic | an embed footer is plain text by definition, so the only way to format that line is to move it into the body (Lucas, 2026-09-16) | 75 goldens; re-recorded after proving the change mechanically: every old transcript, with its footer moved by the same rule, equals the new one exactly, so nothing else moved | panel and cards train |
| ux-49 | stream_elements_commands: the setup review | the review named some of the streamer's commands the way StreamElements writes them (`!altura`), leaving the admin to work out what to type here | a third field, "My version", lists the same commands with Keiko's own prefix (`ks!altura`), so both forms sit side by side | cosmetic | the command an admin will type in Discord is the one they need to recognise (Lucas, 2026-09-16) | none (the golden's StreamElements stub answers with no command names, so no line is drawn); pinned by `test_the_stream_elements_lookup_counts_enabled_commands` | panel and cards train |
| ux-48 | welcome_messages: the Preview button and the banner on the card | Preview answered nothing at all while the card was open, and the banner kept showing the design's example after a custom picture was sent | Preview draws the message that is on the card, and the banner line shows the picture just sent whenever the chosen design is drawn over one | behavioural, a bug fix | found by Lucas on the local bot (2026-09-16): while a card is open every answer lives as one draft under the card's key, and the preview resolved that draft against the steps, finding nothing | none (no golden path presses Preview); pinned by `test_the_preview_of_a_card_reads_what_is_written_on_it` and `test_the_card_shows_the_picture_that_was_just_sent` | panel and cards train |
| ux-47 | the Preview of a Twitch notification for a streamer who is offline | the embed arrived with nothing where the live picture goes, so nothing showed the space it fills | the preview fills it with the last stream's picture, and with the channel's offline picture when there is no recording | cosmetic | a preview should show the shape of the message, not only its words (Lucas, 2026-09-16) | none (no golden path presses Preview); pinned by `test_the_twitch_preview_shows_an_example_when_nobody_is_live` | panel and cards train |
| ux-46 | block_links, and any panel whose list shares a heading with another setting: opening the command | the command did not open at all, Discord refused the whole panel with "Component custom id cannot be duplicated" (50035): the button ux-41 put beside an empty list encoded the same Add action, with no argument, as the Add button below the card | the button beside a list names the list it adds to, so its id is its own, and the panel opens | behavioural, a bug fix | found by Lucas on the local bot (2026-09-16), one screen away from ux-41 | none (every golden seeds its list with items); pinned by `test_a_panel_whose_list_is_empty_has_no_duplicated_custom_id`, which fails on the very panel Discord refused | panel and cards train |
| ux-45 | stream_elements_commands: the setup review | how many commands were found was buried in the paragraph above the streamer, and the card carried Keiko's own picture | the paragraph says what the feature does; under the streamer sit a field with how many commands are active and another naming some of them, each shown only when StreamElements answered, which withdraws the outage sentence ux-24 promised: an unreachable StreamElements now leaves the fields out rather than explaining itself; the card carries the streamer's Twitch picture | behavioural | a number an admin checks before saving is a field, not a sentence (Lucas, 2026-09-16); a review step can now declare lines fed by a looked up answer, shown on the card and never saved | the 3 stream_elements_commands goldens that draw the review; re-recorded after reading it: the description line, the thumbnail and the added "Active commands" field, and nothing else | panel and cards train |
| ux-44 | the Preview button of a Twitch or YouTube notification | the preview showed the written message alone, so nothing said what the announcement itself would look like | the preview carries the very embed the announcement sends: the streamer's or channel's picture, name and description; on Twitch the live's own title and category when there is one, and an example of both when nobody is live; on YouTube the video lines are an example, since the next video does not exist yet | behavioural | the text is the smaller half of a notification, and the half nobody could check before saving (Lucas, 2026-09-16) | none (no golden path presses Preview); pinned by `test_the_twitch_preview_carries_the_embed_the_notification_sends`, `test_the_twitch_preview_shows_an_example_when_nobody_is_live` and `test_the_youtube_preview_carries_the_embed_the_notification_sends` | panel and cards train |
| ux-43 | welcome_messages: the "Banner design" line of the setup card | the line named the chosen design ("Server Image") and showed nothing else; the only way to see it was to open the gallery | the line carries the banner of the chosen design, drawn with the saved title, and the button that opens the gallery sits under the image; the Preview button stays and still sends the whole message | behavioural | a design is picked by looking at it (Lucas, 2026-09-16); the card waits for the drawing only within a budget (`ViewConstants.PREVIEW_WAIT_SECONDS`), so a slow drawing costs the screen nothing, while the gallery itself still waits as long as it takes | the 7 welcome_messages goldens that draw the card; re-recorded after checking that every added line is the media block of the design section, and that the only other changes are the heading of ux-41 and the defer the gallery click no longer needs, because the previews are already drawn by the time it opens; pinned by `test_the_card_shows_the_banner_of_the_chosen_design` and `test_a_card_never_waits_longer_than_the_preview_budget` | panel and cards train |
| ux-42 | default_roles: the question the Sync button asks | it named the roles every member and every bot would end up with, but never said how many people it was about to touch | it says, for members and for bots, how many will get a role and which roles; the count is only who does not have the role yet, and only roles Keiko can actually assign | cosmetic, copy | an action that writes to every member should say its size before it runs (Lucas, 2026-09-16) | none (no golden path presses Sync); pinned by `test_an_aside_that_asks_first_spends_its_cooldown_only_when_confirmed` | panel and cards train |
| ux-41 | every manager panel and review: the heading of each block, and the "Exceptions" block of block_links | each heading repeated the emoji its own first line already carried ("### 🚦 Link Settings" right above "🚦 **Blocking mode:**"), an empty list sharing a heading printed its "None" on a second line with no button beside it, and both Exceptions buttons did nothing at all: the panel gives a card field its own Edit whenever the field declares a heading of its own, but the engine honoured a part target only on a card that also declares `edit_by_field`, so the click answered a stale notice and redrew the panel | the heading is the title alone and every line keeps its own icon; an empty list reads "Your links: None" on one line; "Free a famous website" and "Free specific links" both open what they name, and the one with no link yet adds instead of editing | behavioural, a bug fix | found by Lucas on the local bot (2026-09-16): the panel emojis piled up and the exceptions button did nothing | the 41 goldens that draw a panel or a review; re-recorded after checking that each of the 74 changed lines is a heading losing its emoji and nothing else (no golden seeds an empty list, so the Add button is pinned by unit tests instead); pinned by `test_the_edit_beside_a_declared_group_opens_that_part`, `test_a_group_heading_carries_no_emoji_of_its_own`, `test_a_list_with_no_item_reads_on_one_line`, `test_an_empty_list_of_a_declared_group_offers_add_instead_of_edit` and `test_a_list_with_no_items_is_never_opened_by_an_edit` | panel and cards train |
| ux-40 | reminders_birthday: the default celebration message, on the card, the review and the message the server gets on the day | the message closed with "🎉🐾" (the paw Keiko signs errors and status lines with) | it closes with "🎉✨" | cosmetic, copy | the paw reads as Keiko talking about herself; the birthday line is about the member (Lucas, 2026-09-16) | the 8 reminders_birthday goldens that show the default message; re-recorded after checking the diff changes nothing but the emoji (41 lines changed, all of them the same swap) | panel and cards train |
| ux-7 | Components V2 cards (block_links link settings, birthday cards), after Back from the next step | the card comes back with every value shown as `-`: Back rebuilt the card from an empty state, so the answers the admin had just given, and the defaults, disappeared from the screen (the values were still saved) | the card comes back showing the values it had | behavioural, a bug fix | the card is a pure render of the session's answers (review I3, I7) | `block_links/setup_back` | Phase D |
| ux-5 | adding an item from a Components V2 card (block_links from the panel, birthday member card) | the final "item added" embed cannot be edited into the card (Discord refuses embeds on a Components V2 message), so the bot sends it as a new message and the card stays on screen with buttons that no longer work | the card is replaced by the "item added" message | behavioural, a bug fix | the renderer decides embed vs Components V2 per screen and replaces instead of editing (review H8, H12) | `block_links/manager_add_item`, `reminders_birthday/manager_add_item` | Phase D |
| ux-60 | the member filter of a records list (blocked links, both history screens) | Confirm with nobody chosen answers nothing, so Discord shows "This interaction failed", and the screen has no way back | Confirm with nobody chosen shows every record, and a Cancel button returns to the full list | behavioural, a bug fix | a click on the picker always ends in a screen (review F1) | none: the picker has no golden | Phase D |
| ux-61 | every screen that lists a setting: the review, the card and the manager panel | an empty setting reads "None" on the panel and "-" on the card and the review, because each screen answered the question on its own | every one of them reads the same: "None" in English, "Nada ainda" in Portuguese, which fits a masculine or a feminine setting name | cosmetic, finishing ux-13 | ux-13 promised None for an empty exception list and only the panel got it (review B2) | block_links setup_happy (both locales) and the seven reminders_birthday transcripts that show an empty birthday message | Phase D |
| ux-62 | reminders_birthday, the member select of the birthday list, opened by Edit and by Remove | Edit opened a dropdown with one option per item, which for a list keyed by member is a list of mentions Discord refuses to draw when it is empty | Edit opens the member select directly, the same screen Remove already used, and that screen's Cancel becomes Back, which returns to the panel instead of asking whether to discard | behavioural, a bug fix | the list says it is keyed by a member, so the picker that answers it is the member one (review B8) | `reminders_birthday/manager_remove_item`; no golden walks the birthday Edit | Phase D |
| ux-63 | every setup review that lists items (block_links up to 25 links, reminders_birthday up to 25 birthdays) | the review listed every item, however many there were | the review shows the first 10 and a "+N more" line, the same cap the manager panel already used, because the two screens now share one block builder | behavioural | one screen, one builder: a list that is too long for the panel is too long for the review, and Discord's size limit is the same on both | none; no golden has more than a few items | Phase D |
| ux-64 | welcome_messages, the design gallery reached from the manager panel's Edit | the gallery opened with no banners: the manage open returned no pending previews, so every design card fell back to no picture | the gallery draws the same banners the setup gallery draws; the render starts in the background when the panel opens and is cancelled when the session closes | behavioural, a bug fix | a design is chosen by looking at it, and the panel is where a saved design is changed | none; no golden walks the manager to the gallery | Phase D |
| ux-65 | every manager panel and setup review of a form with a card: the welcome banner's custom image, block_links' allowed popular websites | the panel listed every field of the card, even one whose section the card itself hides, so a value left behind by a changed answer kept a line; `welcome_custom_image` was marked hidden to keep that line away, and lost its own Edit with it, leaving no way to change the banner image after setup | a card key is on the panel exactly while the section that owns it is visible, and while it is there it carries its own Edit: the custom image comes back with its Edit under a custom design and goes away under the server one, and "Allowed popular websites" goes away in allow-all mode | behavioural, a bug fix | the panel shows what the card shows (decided with Lucas, 2026-09-22) | none; no golden seeds a card whose section is hidden with a value behind it; pinned by the two scenarios of `tests/behavioral/regressions/test_panel_follows_the_card.py` | Phase D |

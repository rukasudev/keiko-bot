# UX changes of the form platform migration

The golden transcripts under `tests/behavioral/golden/` are the record of what an
admin sees today, path by path. A migration of the engine is invisible to admins
unless a change is listed here first (review II.3). Every row names the golden it
moves; the golden test accepts the row's id in `allowed=` and nothing else.

Classes: `invisible` (ids, internal encodings: never listed, the normalizer drops
them), `cosmetic` (a footer, an icon), `behavioural` (a new notice, a different
message count, a different choreography).

| id | form / screen | before | after | class | reason | golden affected | since |
|---|---|---|---|---|---|---|---|
| ux-1 | every form, any button or select | a click on an outdated screen is routed to the current handler (deterministic ids) or fails silently (random ids); a double click on Done / Confirm runs the step twice | a short ephemeral notice ("this screen is out of date, here is the current one") and the current screen is redrawn once; the second click of a double click is ignored | behavioural | dedup by interaction id and revision in the adapter (review 4.6, plan 3.5) | none of the recorded paths clicks twice or on an old screen; new scenarios in Phase C | Phase D |
| ux-2 | every form, after 30 minutes without a click | the buttons stay on the message and every click fails with "This interaction failed" | the message loses its buttons and says the form expired, in the admin's locale | behavioural | expiry becomes a visible transition (review C2, H6) | none of the recorded paths waits for a timeout; `scenario.expire()` in Phase C | Phase D |
| ux-3 | options steps, confirming with nothing selected | a public `channel.send` in the channel where the command ran | an ephemeral error embed, like every other validation error | behavioural, a bug fix | one error channel (review G13) | none of the recorded paths confirms an empty options step | Phase D |
| ux-4 | every transition from a card or gallery back to an embed screen | the old message is deleted first, then the new one is sent; when the delete fails the admin loses the screen (H11) | the new message is sent first, then the old one is deleted | invisible when it succeeds, behavioural when the delete fails | one replacement choreography in the effect executor (review P3, H11) | every golden with `delete` before `followup_send`: block_links edit and remove, default_roles edit, twitch and youtube add, edit and remove, birthday edit and remove, every manager pause / disable | Phase D |
| ux-6 | welcome_messages, the design gallery | the gallery's buttons read Cancel, Back: the one screen where Cancel is not last | Back, Cancel, like every other screen | cosmetic | one button order for every screen (`cancel_last`) | `welcome_messages/setup_happy`, `setup_custom_image`, `setup_back`, `setup_cancel_keep`, `setup_cancel_discard`, `manager_edit_one_step` | Phase D |
| ux-8 | the review screen, values of `bullet` style (twitch and youtube notification messages, welcome messages) | the review lists them numbered (`1.`, `2.`) while the panel lists the same values with bullets: two formatters with the styles swapped | bullets on the review too | cosmetic, a bug fix | one formatter per style (review G6) | `notifications_twitch/setup_happy`, `notifications_youtube_video/setup_happy`, `welcome_messages/setup_happy` | Phase D |
| ux-9 | modals opened to edit a saved value (twitch and youtube notification messages, stream_elements streamer, birthday day) | some open empty, some open with the saved value: each modal decided on its own | every modal opens with the value it edits | cosmetic | the modal is a render of the session's answers (review I3) | `notifications_twitch/manager_edit_one_step`, `notifications_youtube_video/manager_edit_one_step` | Phase D |
| ux-10 | `/setup` and the greeting's dashboard button | an embed with one bold line per feature, the command on the next line, and six buttons below the list | a Components V2 card with Keiko's picture beside a three-line header (a 🐶 title, a one-sentence intro, and a subtext note saying these are the moderation and notification features and that `/help` has the rest); features grouped under "to set up" first and "configured" second with their counts; each row is the feature's name plus one subtext line with what it does and its command (and "paused" when it is), with a Set up or Manage button beside it; no status circles; the report footer; below the card, outside its frame, a row of Commands (the /help list, privately), History (every feature's recent changes), Permissions (what Keiko is missing for each configured feature) and Support (a link to the official server) buttons | cosmetic | the screen is most servers' first contact with Keiko; a first card redesign said each status three times over three lines, left a blank band beside the picture, and listed what was already done before what was left | none (pinned by tests/behavioral/contracts/test_setup_dashboard.py) | logs and setup train |
| ux-11 | every Components V2 form screen: manager panels, setup cards, the design gallery and pickers | the screen's own buttons (Pause, Disable, Add, History, Help, Done, Back, Cancel) sit inside the card's frame, above the footer | the card holds the content and closes with the footer; the screen's own buttons sit below it, outside the frame, as on `/setup`; a button that belongs to one part of the card (a section's buttons, the Edit beside a group, a design's Select, a picker's options) stays inside | cosmetic | one place for a screen's buttons on every card, the `/setup` layout of ux-10 | 58 goldens, every one with a card: block_links (13), default_roles (5), notifications_twitch (7), notifications_youtube_video (7), reminders_birthday (12), stream_elements_commands (5), welcome_messages (9); re-recorded after passing under a canonicalizer that only folded the rows back into the card | logs and setup train |
| ux-12 | welcome_messages: the design gallery and the banner a new member gets | the "custom image only" example was a 28.3 MB gif on an outside host, slow and at times "image not found"; the blur example waited on a 1 MB download; a server without an icon, or whose custom background link had expired, got no welcome message at all | the example is a 2.4 MB gif served from Discord, the blur example is drawn from a local picture, both banners are drawn together; a server without an icon gets a plain background in the house colour, an expired Discord background link is refreshed before drawing, and any background that still cannot be loaded falls back to the plain one | behavioural | the gallery is an admin's first look at the feature, and a welcome message that silently never arrives is the worst outcome of the feature | none (the goldens count gallery media, not urls; pinned by tests/behavioral/regressions/test_welcome_design_previews.py) | welcome previews train |
| ux-13 | block_links: the mode picker, the card, the review and the panel | the modes read "🚫 Block all, with exceptions" and "✅ Allow all, block only some"; an admin who blocks every link read "with exceptions" as something still to set up | "🚫 Block all" and "✅ Allow all"; the picker line of Block all says exceptions are optional, and the exceptions keep their own lines on the card and the panel (None when empty) | cosmetic | the label described what an admin may add, not the mode | the 13 block_links golden transcripts; re-recorded after passing under a canonicalizer that only folded the labels and the picker text | panel and cards train |
| ux-14 | every form, the dropdown that picks what to edit or remove (manager and review) | the dropdown had no button: the only way back to the panel or the review was to pick something or run the command again | a Back button below the dropdown returns to the panel in the manager and to the review in the setup, without changing anything | behavioural | every picker of a card already had a way back; this one did not | `block_links/manager_remove_item`, `notifications_twitch/manager_edit_one_step` and `manager_remove_item`, `notifications_youtube_video/manager_edit_one_step` and `manager_remove_item`, `reminders_birthday/manager_edit_one_step` and `manager_remove_item`, `stream_elements_commands/manager_edit_one_step`, `welcome_messages/manager_edit_one_step`; re-recorded after passing under a canonicalizer that only dropped the Back button | panel and cards train |
| ux-15 | default_roles: the roles step and the Sync button of the panel | one line said to choose roles for new bots and members, and only the dropdown placeholders told the two apart; confirming with both dropdowns empty saved an empty setup; Sync assigned roles to every member on one click and answered with a bare green embed | the step lists "1. Roles for bots" and "2. Roles for members" with when each applies and says one may stay empty; confirming with both empty shows the selection-required error; Sync first asks, on a private message with Confirm and Cancel, and spends its cooldown only when confirmed; the waiting and result embeds carry Keiko's picture, the house colour, a footer, and the result counts members and bots | behavioural, a bug fix | the only setting of the feature must say what each dropdown is for, and an action that touches every member must say so before it runs | `default_roles/setup_happy` (both locales), `setup_cancel_keep`, `setup_cancel_discard`, `manager_edit_one_step`; re-recorded after passing under a canonicalizer that only folded the step description; the refusal and Sync are pinned by `test_a_required_multi_select_refuses_confirm_when_every_select_is_empty`, `test_an_aside_that_asks_first_spends_its_cooldown_only_when_confirmed` and `test_the_sync_result_reads_like_keiko` | panel and cards train |
| ux-16 | notifications_twitch and notifications_youtube_video: the streamer and youtuber modal; stream_elements_commands: the streamer modal | the placeholder was a bare `shroud` / `pewdiepie` and the hint said "only the username"; a nick typed as `@gaules` was not found | the placeholder reads "your nick (e.g. @shroud)" / "your channel (e.g. @pewdiepie)" and the hint says with or without @; the lookup, the validator and the saved item use the nick without spaces, a leading @ or capitals (`normalize: handle`) | behavioural | the placeholder invites the @, so the @ must work | `notifications_twitch` and `notifications_youtube_video` `setup_happy` (both locales), `setup_back`, `setup_cancel_keep`, `setup_cancel_discard`, `setup_validation_error_and_recover`; re-recorded after passing under a canonicalizer that only folded the hint and the placeholder; the @ is pinned by `test_a_streamer_typed_with_an_at_sign_is_found_and_saved_without_it` | panel and cards train |
| ux-17 | `/setup`, `/help`, and the saved, paused, unpaused, disabled and history messages of stream_elements_commands | Stream Elements was missing from `/setup`; `/help` never mentioned `/setup`; Stream Elements messages named a command that does not exist (`/stream elements commands`) | `/setup` lists Stream Elements with its command and a Set up or Manage button, and its note says integrations too; `/help` says admins can use `/setup` and carries a Setup button that opens it privately (a member gets the admin-only notice); the messages name the real command (`/integrations streamelements commands`) | behavioural | the integration was a command nobody found, and `/help` is where people look first | the seven stream_elements_commands goldens (`setup_happy` in both locales, `manager_edit_one_step`, `manager_pause`, `manager_unpause`, `manager_disable`, `manager_history`); re-recorded after passing under a canonicalizer that only folded the command name; `/setup` and `/help` are pinned by `tests/behavioral/contracts/test_setup_dashboard.py` | panel and cards train |
| ux-18 | a modal whose value is looked up outside Discord (the Twitch or YouTube name, the StreamElements streamer), in the setup and when edited from the panel | the bot looked the name up before answering the modal, so a slow Twitch or YouTube answer could lose Discord's three seconds; saving an edit of that field showed "Keiko is thinking" first | the modal is answered first (a silent defer) and then the name is looked up; an edit saved from such a modal no longer shows the thinking placeholder before the saved message | behavioural | a lookup can take longer than Discord waits for a modal | `stream_elements_commands/manager_edit_one_step`; re-recorded after passing under a canonicalizer that only dropped the thinking defer; the order is pinned by `test_a_modal_that_needs_a_lookup_is_answered_before_the_lookup_runs` | panel and cards train |
| ux-19 | every manager panel | settings were plain `**Title:** value` lines; only a card, a multi-select or a list had a heading and an Edit beside it, every other step (a channel, a design, a modal) was reached through a global Edit and a dropdown; the intro read "Enabling this feature allows me to..."; the birthday rows had no group and kept the global Edit | the panel opens with `manager_description`, written as what the feature is doing; every step is a block with an Edit beside what it opens; every line leads with its own emoji (the section, select or step icon, the frisbee when none is declared); a one-line block has no heading; the global Edit only remains when a feature row names no step; the birthday rows sit under the settings card and the birthday list | behavioural | an Edit beside what it opens (decided with Lucas, 2026-09-15), and a panel description that reads as the feature being on | the 42 manager goldens of the seven forms; re-recorded after reading each form's new panel (the change is structural: sections, headings and buttons move, so no canonicalizer can fold it); `stream_elements_commands`, `welcome_messages` and `reminders_birthday` `manager_edit_one_step` now click the Edit beside the step instead of the global Edit and its dropdown; pinned by `test_every_visible_step_is_a_panel_group_with_its_own_edit`, `test_every_panel_line_leads_with_its_icon_or_the_frisbee`, `test_a_one_line_group_has_no_heading`, `test_feature_rows_group_by_the_step_that_owns_their_key` and the manager panel limits contract | panel and cards train |
| ux-20 | every setup review ("Alright?") | an embed listing every answer as a frisbee line with its value in bold (a list of messages printed `**` around a code block), an Edit that opened a dropdown of steps, and the saved message edited over it | a Components V2 card with the same blocks as the manager panel: every step a block with an Edit beside what it opens, a line per setting led by its emoji, lists as their items; Add, Remove, Preview, Confirm and Cancel below the card; the Edit beside a block returns to the review; confirming replaces the card with the saved message | behavioural, a bug fix (the bold around a block) | one screen shape for reading settings, whether they are being set up or managed | the 17 setup goldens that reach the review (`setup_happy` of the seven forms in both locales, `welcome_messages/setup_custom_image`, `reminders_birthday` and `stream_elements_commands` `setup_validation_error_and_recover`); re-recorded after reading each form's review card, the change is structural; pinned by `test_the_review_is_a_card_with_the_panel_groups_and_its_buttons_below`, `test_edit_beside_a_review_group_returns_to_the_review`, `test_confirming_a_card_review_replaces_it_with_the_enabled_message`, `tests/behavioral/regressions/test_review_values.py` and the setup review limits contract | panel and cards train |
| ux-7 | Components V2 cards (block_links link settings, birthday cards), after Back from the next step | the card comes back with every value shown as `-`: Back rebuilt the card from an empty state, so the answers the admin had just given, and the defaults, disappeared from the screen (the values were still saved) | the card comes back showing the values it had | behavioural, a bug fix | the card is a pure render of the session's answers (review I3, I7) | `block_links/setup_back` | Phase D |
| ux-5 | adding an item from a Components V2 card (block_links from the panel, birthday member card) | the final "item added" embed cannot be edited into the card (Discord refuses embeds on a Components V2 message), so the bot sends it as a new message and the card stays on screen with buttons that no longer work | the card is replaced by the "item added" message | behavioural, a bug fix | the renderer decides embed vs Components V2 per screen and replaces instead of editing (review H8, H12) | `block_links/manager_add_item`, `reminders_birthday/manager_add_item` | Phase D |
| ux-60 | the member filter of a records list (blocked links, both history screens) | Confirm with nobody chosen answers nothing, so Discord shows "This interaction failed", and the screen has no way back | Confirm with nobody chosen shows every record, and a Cancel button returns to the full list | behavioural, a bug fix | a click on the picker always ends in a screen (review F1) | none: the picker has no golden | Phase D |
| ux-61 | every screen that lists a setting: the review, the card and the manager panel | an empty setting reads "None" on the panel and "-" on the card and the review, because each screen answered the question on its own | every one of them reads the same: "None" in English, "Nada ainda" in Portuguese, which fits a masculine or a feminine setting name | cosmetic, finishing ux-13 | ux-13 promised None for an empty exception list and only the panel got it (review B2) | block_links setup_happy (both locales) and the seven reminders_birthday transcripts that show an empty birthday message | Phase D |
| ux-62 | reminders_birthday, the member select of the birthday list, opened by Edit and by Remove | Edit opened a dropdown with one option per item, which for a list keyed by member is a list of mentions Discord refuses to draw when it is empty | Edit opens the member select directly, the same screen Remove already used, and that screen's Cancel becomes Back, which returns to the panel instead of asking whether to discard | behavioural, a bug fix | the list says it is keyed by a member, so the picker that answers it is the member one (review B8) | `reminders_birthday/manager_remove_item`; no golden walks the birthday Edit | Phase D |
| ux-63 | every setup review that lists items (block_links up to 25 links, reminders_birthday up to 25 birthdays) | the review listed every item, however many there were | the review shows the first 10 and a "+N more" line, the same cap the manager panel already used, because the two screens now share one block builder | behavioural | one screen, one builder: a list that is too long for the panel is too long for the review, and Discord's size limit is the same on both | none; no golden has more than a few items | Phase D |
| ux-64 | welcome_messages, the design gallery reached from the manager panel's Edit | the gallery opened with no banners: the manage open returned no pending previews, so every design card fell back to no picture | the gallery draws the same banners the setup gallery draws; the render starts in the background when the panel opens and is cancelled when the session closes | behavioural, a bug fix | a design is chosen by looking at it, and the panel is where a saved design is changed | none; no golden walks the manager to the gallery | Phase D |
